interface LoginPageType {

}

const LoginPage = (props: LoginPageType) => {
    const {

    } = props

    return (
        <>
            <div>
                Login Page
            </div>

        </>
    )
}

export default LoginPage
